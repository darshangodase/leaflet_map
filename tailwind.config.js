/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './index.html',                // Vite's index file at the root
    './src/**/*.{js,ts,jsx,tsx}',  // All JS, TS, JSX, and TSX files in the src folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};